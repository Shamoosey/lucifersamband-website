export namespace Interfaces {
    export interface NavBarItem {
        title: string,
        icon: string,
        redirect: string,
        external: boolean
    }

    export interface SpotifyAblum {
        img: string,
        meta: string,
        url: string
    }
}
