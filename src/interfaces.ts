export namespace Interfaces {
    export interface NavBarItem {
        title: string,
        icon: string,
        redirect: string
    }

    export interface SpotifyAblum {
        img: string,
        meta:string,
        url: string
    }
}
